# Task Manager — Аудит реального сценария
## Дата: 2026-03-24

Сценарий: ТО-1 (250 моточасов) подошло на МКЗ Генератор 1.
Цель: протестировать полный цикл жизни задачи ТО.

---

## ШАГ 0: Факты системы

### 0.1 Моточасы генераторов (Redis device:N:metrics)
| Device | run_hours | online |
|--------|-----------|--------|
| 1 (МКЗ Генератор 1) | 16908 | True |
| 2 (МКЗ Генератор 2) | 17741 | True |
| 3 (ЯКЗ Генератор 1) | нет данных | True |
| 4 (ЯКЗ Генератор 2) | 16867 | True |
| 5 (МКЗ Генератор 3) | 19974 | True |
| 6 (ЯКЗ Генератор 3) | нет данных | True |

### 0.2 Пороги ТО (maintenance_intervals)
| Шаблон | Интервал | Часы |
|--------|----------|------|
| Стандартный регламент | ТО-1 | 250 |
| Стандартный регламент | ТО-2 | 500 |
| Стандартный регламент | ТО-3 | 1000 |
| Стандартный регламент | ТО-4 | 2000 |

### 0.3 Последние выполненные ТО (maintenance_logs)
| device_id | name | engine_hours | performed_at |
|-----------|------|-------------|--------------|
| 1 | Генератор 1 | 1237 | 2026-02-17 |

**Только 1 запись.** Остальные устройства — без логов ТО.

### 0.4 Текущие задачи (scada_tasks)
**ПУСТО — 0 задач.** Ни одна задача не была создана системой.

### 0.5 Иерархия компании (Redis)
- **company:structure** — НЕ ЗАПОЛНЕНА
- **company:employees** — НЕ ЗАПОЛНЕНА

Руководитель Михайлова и учредитель — **НЕ ОПРЕДЕЛЯЮТСЯ** из Redis.

### 0.6 Ответственные (equipment_responsible)
| equipment_code | equipment_name | responsible_bitrix_id | responsible_name |
|----------------|----------------|----------------------|------------------|
| mkz_motor1 | МКЗ ГПУ Мотор 1 | 104 | Михайлов Алексей |
| mkz_motor2 | МКЗ ГПУ Мотор 2 | 104 | Михайлов Алексей |
| yakz_motor1 | ЯКЗ ГПУ Мотор 1 | 38 | Ратников Сергей |
| yakz_motor2 | ЯКЗ ГПУ Мотор 2 | 38 | Ратников Сергей |

**Таблица заполнена. Михайлов (104) -> МКЗ, Ратников (38) -> ЯКЗ.**

### 0.7 Активные правила (scada_rules)
**ПУСТО — 0 активных правил.** Автоматические действия не настроены.

### 0.8 TaskSupervisor
**НЕ ЗАПУЩЕН.** Логов supervisor/escalation в docker logs нет.
Код существует (task_supervisor.py), но НЕ подключен в main.py lifespan.

### 0.9 Bot
- BITRIX24_BOT_ID=228
- Логов bot/imbot в последних 100 строках нет

---

## ШАГ 1: Создание задачи — проверка цепочки

### 1А: Через Санька (ручная)
Тестовая отправка не производилась (только аудит кода).
Код создания задач существует в task_engine.py:
- create_task() — создает ScadaTask, отправляет в Б24 через tasks.task.add
- Привязка к equipment_responsible — по equipment_code
- bitrix_task_id заполняется при успешном создании в Б24

### 1Б: Автоматическое создание по моточасам
- Код: **hours_monitor.py** — класс HoursThresholdMonitor
- Логика: сравнивает текущие run_hours с порогами из maintenance_intervals
- При достижении порога вызывает task_engine.create_task()
- **НЕ ЗАПУЩЕН в main.py** — нет в lifespan, asyncio.create_task() не вызывается

**Статус: Код ЕСТЬ, но НЕ ЗАПУЩЕН. Задачи автоматически не создаются.**

---

## ШАГ 2: Задача в Б24 — синхронизация

- task_engine.py вызывает tasks.task.add через resilient_b24_call
- GROUP_ID берется из settings (BITRIX24_TASK_GROUP_ID)
- Исполнитель = responsible_bitrix_id из equipment_responsible
- Наблюдатели = из settings
- **Синхронизация работает на уровне кода, но задачи не создавались (scada_tasks пуста)**

---

## ШАГ 3: Коммуникация Санек -> Исполнитель

### Код отправки сообщений
- task_supervisor.py:201 — вызывает imbot.message.add
- Использует resilient_b24_call (с retry и offline queue)
- DIALOG_ID = bitrix user_id исполнителя (из _resolve_target)

### Определение исполнителя
- При escalation level 1-2: берет task.responsible_user_id (из equipment_responsible)
- При level 3: _find_manager() из company:employees
- При level 4: _find_founder() из company:employees

### Проблема
- **company:employees НЕ ЗАПОЛНЕНА** -> _find_manager() вернет None
- Fallback: settings.BITRIX24_FALLBACK_RESPONSIBLE_ID
- Отправка сообщений исполнителю (level 1-2) — должна работать (ID из task.responsible_user_id)
- Отправка руководителю/учредителю — **упадет на fallback**

**Статус: Код ЕСТЬ. Отправка исполнителю — должна работать. Руководитель/учредитель — fallback (company:employees пуста).**

---

## ШАГ 4: Получение ответа исполнителя

### Обработка входящих
- bitrix_bot.py:694 — _handle_incident_response() обрабатывает ответы через keyboard buttons
- Команда incident_response маршрутизируется на строке 810
- Ответ записывается в task_communications (message_type=incident_response)
- Привязка к задаче: по dialog_id / task_id

### Таблица task_communications
- **ПУСТА** (0 записей) — ни одного обмена сообщениями не было

### Свободный текст
- Обработка **только через keyboard buttons** (incident_response)
- Свободный текстовый ответ (типа "ок") — **НЕ ОБРАБАТЫВАЕТСЯ** как ответ на задачу
- Исполнитель должен нажать кнопку, а не просто написать текст

**Статус: Код ЕСТЬ (через кнопки). Свободный текстовый ответ НЕ привязывается к задаче.**

---

## ШАГ 5: Анализ ответа (LLM)

- LLM-анализ содержательности ответа: **НЕТ**
- grep по analyze_response, substance, по существу, relevance, evaluate_response — **пусто**
- Нет кода, который бы оценивал "ответил ли исполнитель по существу"
- communication_strategy / adaptive communication: **НЕТ**

**Статус: ОТСУТСТВУЕТ. Нет LLM-анализа ответов. Система не различает "ок" от содержательного ответа.**

---

## ШАГ 6: Цепочка эскалации

### Уровни (task_supervisor.py)
| Уровень | Название | Целевой получатель | Триггер |
|---------|----------|-------------------|---------|
| 1 | soft_reminder | executor | deadline приближается (ESCALATION_SOFT_REMINDER_HOURS) |
| 2 | hard_reminder | executor | deadline просрочен |
| 3 | manager_escalation | manager | просрочено > ESCALATION_HARD_HOURS и нет ответа |
| 4 | founder_escalation | founder | просрочено > 24ч и руководитель не ответил |

### Определение руководителя
- Из company:employees -> _find_manager(employees, task.responsible_user_id)
- Ищет manager_id у сотрудника -> находит запись с таким id
- **company:employees ПУСТА** -> всегда fallback на BITRIX24_FALLBACK_RESPONSIBLE_ID

### Определение учредителя
- Из company:employees -> _find_founder() — ищет weight >= 0.9
- **company:employees ПУСТА** -> fallback

### Запуск
- **TaskSupervisor НЕ ЗАПУЩЕН** в main.py -> эскалация не работает вообще

**Статус: Код ПОЛНЫЙ. Логика правильная. НЕ ЗАПУЩЕН. company:employees пуста -> fallback.**

---

## ШАГ 7: Контроль качества

### quality_control.py
- Код ЕСТЬ: run_quality_checks() вызывается при завершении задачи
- Чек-лист: проверяет выполнение checklist_items из MaintenanceCard
- Метрики до/после: capture_metrics_snapshot("before") при создании, schedule "after" при завершении
- Записи в task_quality_checks: создаются

### photo_verification.py
- Код ЕСТЬ: использует Vision API (GPT-4o) для анализа фото
- Проверяет фото до/после ТО по checklist_items
- photo_before_required, photo_after_required, vision_check

### task_learning.py
- Код ЕСТЬ: анализирует quality_score выполненных ТО
- Находит low_quality задачи (score < 0.5)
- Обучение на основе результатов

### Текущие данные
- task_quality_checks: **ПУСТА** (0 записей)
- Ни одна задача не проходила контроль качества

**Статус: Код ПОЛНЫЙ (quality_control + photo_verification + learning). Не тестировался (нет задач).**

---

## ИТОГ: Что работает, что нет

| Этап | Статус | Комментарий |
|------|--------|-------------|
| Создание задачи (ручное) | ⚠️ | Код есть (task_engine), не тестировалось |
| Создание задачи (авто, по моточасам) | ❌ | Код есть (hours_monitor), НЕ ЗАПУЩЕН в main.py |
| Синхронизация Б24 | ⚠️ | Код есть (tasks.task.add), не тестировалось (нет задач) |
| Коммуникация -> исполнитель (level 1-2) | ⚠️ | Код есть (imbot.message.add), supervisor НЕ ЗАПУЩЕН |
| Ответ исполнителя -> система | ⚠️ | Только через кнопки. Свободный текст НЕ обрабатывается |
| Анализ ответа (LLM) | ❌ | ОТСУТСТВУЕТ. Нет кода оценки содержательности ответа |
| Эскалация -> руководитель | ❌ | Код есть, supervisor НЕ ЗАПУЩЕН + company:employees пуста |
| Эскалация -> учредитель | ❌ | Код есть, supervisor НЕ ЗАПУЩЕН + company:employees пуста |
| Контроль качества | ⚠️ | Код полный (quality + photo + learning), не тестировался |

---

## ЧТО НУЖНО ДОПИСАТЬ / ПОЧИНИТЬ

### Критические (блокируют весь цикл)
1. **Запустить TaskSupervisor в main.py lifespan** — добавить create_task() для TaskSupervisor.start(), аналогично poller/scheduler. Сложность: низкая (5-10 строк).
2. **Запустить HoursThresholdMonitor в main.py lifespan** — аналогично. Сложность: низкая.
3. **Заполнить company:employees в Redis** — нужен механизм синхронизации сотрудников из Б24 (bitrix24 user.get / department.get). Без этого эскалация на руководителя/учредителя не работает. Сложность: средняя.

### Важные (функциональные пробелы)
4. **LLM-анализ ответа исполнителя** — код отсутствует. Нужно: получить ответ -> отправить в LLM -> оценить "по существу или нет" -> решить: принять / эскалировать. Сложность: средняя.
5. **Обработка свободного текста от исполнителя** — сейчас только keyboard buttons. Нужно: если исполнитель отвечает текстом в чат бота -> привязать к задаче -> записать в task_communications. Сложность: средняя.
6. **company:structure** — тоже пуста, хотя для task_supervisor используется company:employees. Нужно понять: это разные кеши или один должен заменять другой. Сложность: низкая (анализ).

### Желательные (улучшения)
7. **Тестовый прогон полного цикла** — после запуска supervisor и hours_monitor, создать тестовую задачу и провести через все этапы. Сложность: средняя.
8. **scada_rules пуста** — если нужны автоматические правила (rule_engine), их нужно создать через API/миграцию. Сложность: низкая.
